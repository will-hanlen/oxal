oxal--mesh vine: collapseable kids for faster rendering of large tree.
trees start off collapsed.

---

copy new .dojo.sh from ut

---

make a real blog app, then modify its form config to prove that inplace migrations work

---

give every mesh a vine? this would certainly make it more useful...

---

render doctests fully

---

the semantics of bumping are wrong i think.

---

implement $shape schemas

  static definition of what places below are allowed to have data.
  the most important one is out.form, because then we can build
  smart forms that limit what can be put in the data tree.

---

gall subscriptions for all changes in a mesh

---

serialize $data to tarball

---

sauc can be $hoon or @t. do this at the same time that you implement refracking in xmfs

---

delete $meta nodes if no subdata and all lifes below are 0

---

before attempting to optimize the acer engine, finish it
and have it fully teste

---

mirror remote faucets via grow
- calculate indexes and publish them
- the acer tracks which mirrors exist.
- the acer optimizes by only holding the shortest
  mirror path necessary. so that it can use /[remote]/some/path
  as the real data tree.